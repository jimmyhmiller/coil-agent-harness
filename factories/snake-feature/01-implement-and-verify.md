# Worker: Implement and verify wraparound Snake

Inspect the existing native GUI Snake workspace. Implement the feature request in
the authoritative Coil engine without moving game rules into native code. Add the
four explicitly named behavioral tests, preserve self-collision and restart, update
the README, and run the actual check, test, and clean build commands. Fix failures
instead of merely reporting them. Call `report_worker_status` only after the files
and commands provide concrete evidence that the feature works.
