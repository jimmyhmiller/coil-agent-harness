# Worker: implement

You have file and shell tools available: `read_text_file`, `write_text_file`,
`edit_text_file`, `create_directory`, `search_text`, and `bash`. Use them. Do not
claim a tool is unavailable without attempting to call it first, and do not report
your status until you have actually read and edited files.

Give the context found on this issue, please implement a fix fully. If there are existing tests and you think this issue could use tests around it, add them. If there are not, do not bother. Make sure to follow conventions, ensure things build and that lints pass.
